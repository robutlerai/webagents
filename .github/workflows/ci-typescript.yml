name: "CI: TypeScript SDK"

on:
  push:
    branches: [main, develop]
    paths: ['typescript/**']
  pull_request:
    branches: [main, develop]
    paths: ['typescript/**']

defaults:
  run:
    working-directory: typescript

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Both supported LTS lines. The portal runs 24; the published SDK has
        # no native deps and no technical need for it, so we keep 22 working
        # for third-party consumers until it reaches EOL on 2027-04-30. This
        # matrix must stay in sync with the `engines` floor in package.json.
        node-version: ['22', '24']

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9

    - name: Install dependencies
      run: pnpm install --frozen-lockfile

    - name: Lint
      run: pnpm run lint

    - name: Type check
      run: pnpm run typecheck

    - name: Run tests
      run: pnpm run test:run

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'

    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9

    - name: Install and build
      run: |
        pnpm install --frozen-lockfile
        pnpm run build

    - name: Check package
      run: pnpm pack --dry-run
